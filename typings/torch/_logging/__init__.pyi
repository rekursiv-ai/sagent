from ._internal import (
    DEFAULT_LOGGING as DEFAULT_LOGGING,
    LazyString as LazyString,
    _init_logs as _init_logs,
    dtrace_structured as dtrace_structured,
    get_structured_logging_overhead as get_structured_logging_overhead,
    getArtifactLogger as getArtifactLogger,
    hide_warnings as hide_warnings,
    set_logs as set_logs,
    trace_structured as trace_structured,
    warning_once as warning_once,
)
